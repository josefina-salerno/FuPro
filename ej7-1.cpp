#include <iostream>
#include <cmath>
using namespace std;
int main(){
    float a, b, c, x, calculo1,calculo2,calculo3;
    cout<<"Ingrese a, b, c, x"<<endl;
    cin>>a>>b>>c;
    calculo1=(-b+sqrt(b*b-4*a*c))/2*a;
    calculo2=log(2*x-1);
    return 0;
}
